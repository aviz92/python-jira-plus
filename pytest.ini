[pytest]
log_cli = true
log_cli_level = DEBUG
testpaths = tests
addopts = --strict-markers
markers =
    integration: real Jira Cloud connection required (needs JIRA_USER_NAME, JIRA_TOKEN, JIRA_BASE_URL)
